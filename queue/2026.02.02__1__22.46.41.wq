 选择：B. 保留你写的 [3.7, 3.12]，但 lock 做“条件锁”（仍然很简）
Pillow==10.4.0; python_version>="3.8"
Pillow==9.5.0; python_version=="3.7"  ； cffi==1.15.1; python_version=="3.7"

cffi==1.16.0; python_version>="3.8"

pycparser==2.21（cffi 的常见依赖，纯 Python，锁了省心） ； 不要：我把 ARM64 Windows 明确走 L2/L3（或判不支持），L4 只覆盖 win32 的 x86/x64 + Linux/mac 常见架构（从简、稳） 。 给完整版下载地址，基于：上传文件
  